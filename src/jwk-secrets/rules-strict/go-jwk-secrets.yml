rules:
  - id: go-jwk-private-material
    message: "Hardcoded private JWK material in Go (strict)"
    severity: ERROR
    languages: [go]
    paths:
      include:
        - "**/*.go"
    metadata:
      category: security
      cwe:
        - CWE-798
      confidence: HIGH
      likelihood: MEDIUM
      impact: HIGH
      description: >-
        Detects map literals in Go that look like JWKs with private fields
        or regex fallbacks (strict profile). Canonical rule pack:
        https://github.com/Endor-Solutions-Architecture/scripts/tree/main/opengrep_jwk_private_material
      display-name: Go hardcoded private JWK (strict)
      endor-category: vulnerability
      rule-origin-note: github-esa-scripts-opengrep-jwk-private-material
      endor-tags:
        - jwk
        - cwe-798
        - hardcoded-credentials
        - go
        - secret-detection
        - jwk-profile-strict
      technology: [go]
      references:
        - https://datatracker.ietf.org/doc/html/rfc7517
        - https://datatracker.ietf.org/doc/html/rfc7518
    pattern-either:
      - patterns:
          - pattern: |
              $M := map[string]any{"kty": "RSA", ..., "d": $D, ...}
          - metavariable-regex:
              metavariable: $D
              regex: '^"[A-Za-z0-9_-]{24,}"$'
      - patterns:
          - pattern: |
              $M := map[string]any{"kty": "EC", ..., "d": $D, ...}
          - metavariable-regex:
              metavariable: $D
              regex: '^"[A-Za-z0-9_-]{24,}"$'
      - patterns:
          - pattern: |
              $M := map[string]any{"kty": "OKP", ..., "d": $D, ...}
          - metavariable-regex:
              metavariable: $D
              regex: '^"[A-Za-z0-9_-]{24,}"$'
      - patterns:
          - pattern: |
              $M := map[string]any{"kty": "RSA", ..., "p": $P, ..., "q": $Q, ..., "dp": $DP, ..., "dq": $DQ, ...}
          - metavariable-regex:
              metavariable: $P
              regex: '^"[A-Za-z0-9_-]{24,}"$'
          - metavariable-regex:
              metavariable: $Q
              regex: '^"[A-Za-z0-9_-]{24,}"$'
          - metavariable-regex:
              metavariable: $DP
              regex: '^"[A-Za-z0-9_-]{24,}"$'
          - metavariable-regex:
              metavariable: $DQ
              regex: '^"[A-Za-z0-9_-]{24,}"$'
      - patterns:
          - pattern: |
              $M := map[string]any{"kty": "oct", ..., "k": $K, ...}
          - metavariable-regex:
              metavariable: $K
              regex: '^"[A-Za-z0-9_-]{24,}"$'
